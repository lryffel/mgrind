import { describe, it, expect } from 'vitest';
import ExerciseShell from './ExerciseShell.svelte';

describe('ExerciseShell', () => {
  it('is a component', () => {
    expect(ExerciseShell).toBeDefined();
  });
});
